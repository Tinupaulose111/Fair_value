name: Drift Detection (Weekly)

on:
  schedule:
    - cron: "0 2 * * 0"   # Sunday 02:00 UTC
  workflow_dispatch:

permissions:
  contents: read
  actions: write   # Needed to trigger retrain workflow

jobs:
  drift-check:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-locked.txt || pip install -r requirements.txt

      - name: Run Drift Detection
        env:
          GHCR_PAT: ${{ secrets.GHCR_PAT }}      # ✅ Correct
          REPO_OWNER: tinupaulose111
          REPO_NAME: Fair_value
          TRAINER_WORKFLOW: retrain.yml
          TARGET_BRANCH: main
        run: |
          python drift/drift_monitor.py

